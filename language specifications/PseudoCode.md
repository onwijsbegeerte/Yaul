
```
    public: (fn LizzFuzz(int input) => string){
        if((input eq== 5 ) => return 'buzz' )
        .if((input lteq<= 3) => return 'fizz')
        .if((input gteq>= 8) => return 'lizz')
        .if((input lteq<= 3 (and&&) input eq== 5) => 'fizzlizz')
        /(return 'void');
    }

    public: (fn Main (sting[] input) => abyss){
        letVfn LocalFunctionFoundInGlobal be=//Ex (*find(LizzFuzz(int) => string), (throw (this + "Not Found"));
        
        letLfn LocalFunctionInLocal be= priv: ((fn int 1 ... 100) => *fn<abyss>(string input)) >>  
            *.System.Console.WriteLine(LocalFunctionFoundInGlobal//(9));

        let result be= LocalFunctionFoundInGlobal//(9);
        *.System.Console.WriteLine(result);
    }

    public: (unit ObjectLikeStructure(string _a, string _b, fn<abyss>(string input) _function)){
        _a.Close((for *))
        _b.Open((for i:test))
        _function.ReadOnly(())


    }
```

# Variables

string = 'kafka'
bool = true
int = 10
float = 1.15

# 

## if

if can be chained using .if is none of them are true /(do something) can be used to close the if

```
  if((input eq== 5 ) => return 'buzz' )
        .if((input lteq<= 3) => return 'fizz')
        .if((input gteq>= 8) => return 'lizz')
        .if((input lteq<= 3 (and&&) input eq== 5) => 'fizzlizz')
        /(return 'void');
```